<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Delete",
  task_path(@task),
  class: 'btn btn-primary',
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%= link_to 'back to list', tasks_path, class: 'btn btn-primary'  %>
<%= link_to 'Edit this Task', edit_task_path, class: 'btn btn-primary'  %>
